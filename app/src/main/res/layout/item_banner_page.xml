<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- 轮播页面容器 -->
    <View
        android:id="@+id/banner_page_background"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@color/primary"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- 轮播页面内容 -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="24dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <!-- 轮播页面标题 -->
        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/banner_page_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:gravity="center"
            android:textAppearance="?attr/textAppearanceHeadlineSmall"
            android:textColor="@color/on_primary"
            android:textStyle="bold"
            tools:text="轮播标题 1" />

        <!-- 轮播页面描述 -->
        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/banner_page_description"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textAppearance="?attr/textAppearanceBodyMedium"
            android:textColor="@color/on_primary"
            android:alpha="0.8"
            tools:text="这是轮播页面的描述文字" />

        <!-- 轮播页面序号 -->
        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/banner_page_index"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginTop="16dp"
            android:background="@drawable/banner_page_index_bg"
            android:gravity="center"
            android:textAppearance="?attr/textAppearanceTitleLarge"
            android:textColor="@color/primary"
            android:textStyle="bold"
            tools:text="1" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout> 